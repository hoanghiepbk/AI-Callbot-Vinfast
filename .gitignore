.venv/
.venv-*/
__pycache__/
*.pyc
.env
*.onnx
models/*.bin
# Converted ASR weights (heavy) — rebuilt locally via scripts/setup_asr.py, never committed.
models/phowhisper-medium-ct2/
# Piper voice (heavy .onnx) — downloaded via scripts/setup_tts.py, never committed.
models/piper/
scenarios/audio/*.wav
# Raw recordings (real voices, heavy) are private — never commit audio binaries.
# Transcripts (.reference.txt), scenarios/audio/manifest.json and eval/wer_results.json
# stay committed so WER/CER remain verifiable + reproducible without the raw audio.
audio/*.wav
audio/*.mp3
audio/*.m4a
audio/*.reference.txt
.DS_Store
*.egg-info/
build/
dist/
eval/results.json
# Gradio flagging / cache dir created at demo runtime.
.gradio/

# Claude config — local, không commit
CLAUDE.md
CLAUDE.local.md
.claude/
*.epub