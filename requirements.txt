# Pinned at install time — replace <pin-at-install> with the resolved stable version.
# Append-only, keep alphabetical (WORKFLOW §5). Do NOT guess versions.
faster-whisper==<pin-at-install>
langchain-core==<pin-at-install>
langgraph==<pin-at-install>
numpy==<pin-at-install>
ollama==<pin-at-install>
pydantic==<pin-at-install>
silero-vad==<pin-at-install>
sounddevice==<pin-at-install>
